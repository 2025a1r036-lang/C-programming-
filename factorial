#include<stdio.h>
int main(){
      int n;
  printf("enter n ");
  scanf("%d",&n);
  for(int num=0; num<=n; num++){
  int fact=1;
  for (int i=1; i<=num; i++){
      fact=fact*i;
  }
  printf("Factorial is %d is %d\n ",num,fact);
  }
      return 0;
}
