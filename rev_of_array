#include<stdio.h>
int main(){
  int size ;

  scanf("%d",&size);
int l1[size],l2[size];
  for(int i=0; i<size; i++){
   
    scanf("%d",&l1[i]);
  }
     
  for(int i=0; i<size; i++){
       l2[i]=l1[size-i-1];
   printf("%d ",l2[i]);
 }
  return 0;
}
